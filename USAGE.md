# 使用指南 📖

本文档提供详细的使用说明和最佳实践。

## 目录
- [环境准备](#环境准备)
- [基础使用](#基础使用)
- [高级功能](#高级功能)
- [数据解读](#数据解读)
- [实战案例](#实战案例)
- [故障排除](#故障排除)

## 环境准备

### 1. 安装MetaTrader 5

1. 下载并安装MT5终端
2. 注册并登录交易账户（模拟账户也可以）
3. 确保终端保持运行状态

### 2. 安装Python依赖

```bash
# 安装必需的包
pip install MetaTrader5 pandas matplotlib

# 验证安装
python -c "import MetaTrader5 as mt5; print(mt5.__version__)"
```

### 3. 验证MT5连接

```bash
# 运行数据可用性检查
python check_h4_data_availability.py
```

如果看到"✅ MT5连接成功"，说明环境配置正确。

## 基础使用

### 快速分析所有比值

最简单的使用方式，一键生成所有分析：

```bash
python analyze_all_ratios.py
```

**输出内容：**
- 控制台显示每个比值的统计信息
- 生成综合图表PNG文件
- 生成5个CSV数据文件
- 生成汇总报告CSV

**适用场景：**
- 每日/每周市场概览
- 快速了解市场整体状态
- 生成定期报告

### 专项分析金银比

如果只关注金银比：

```bash
python gold_silver_ratio_chart.py
```

**特点：**
- 专注于金银比的详细分析
- 更大的图表尺寸
- 更详细的统计信息

**适用场景：**
- 贵金属投资者
- 深入研究金银关系
- 寻找金银套利机会

### 交互式分析

需要自定义分析参数时：

```bash
python market_ratio_analyzer.py
```

**交互流程：**
1. 选择要分析的比值（1-5或名称）
2. 输入时间范围（天数，默认1825天=5年）
3. 输入时间周期（H1/H4/D1等，默认H4）

**示例：**
```
请选择要分析的比值: 1
请输入时间范围(天数): 3650  # 10年
请输入时间周期: D1  # 日线
```

## 高级功能

### 自定义时间范围

修改脚本中的 `days` 参数：

```python
# 在 analyze_all_ratios.py 中
days = 3650  # 改为10年

# 或在 gold_silver_ratio_chart.py 中
days = 365   # 改为1年
```

**推荐时间范围：**
- 短期分析：180-365天（半年到1年）
- 中期分析：730-1825天（2-5年）
- 长期分析：3650天以上（10年+）

### 自定义时间周期

修改 `timeframe_str` 参数：

```python
timeframe_str = "H1"   # 1小时
timeframe_str = "H4"   # 4小时（默认）
timeframe_str = "D1"   # 日线
timeframe_str = "W1"   # 周线
```

**周期选择建议：**
- H1：短线交易，数据量大
- H4：中短线，平衡精度和数据量（推荐）
- D1：中长线，数据更平滑
- W1：长线投资，看大趋势

### 添加自定义比值

在 `market_ratio_analyzer.py` 中添加新的比值配置：

```python
RATIO_PRESETS = {
    # 现有配置...
    
    "铜金比": {
        "symbol1": "COPPER",  # 铜的品种代码
        "symbol2": "XAUUSD",
        "name1": "铜",
        "name2": "黄金",
        "description": "工业金属vs避险金属，经济周期指标"
    },
}
```

## 数据解读

### 理解统计指标

#### 1. 当前值
比值的最新数值，基于最近一根4小时K线的收盘价计算。

#### 2. 平均值（均值）
历史数据的算术平均值，代表比值的"正常"中枢水平。

#### 3. 标准差
衡量比值的波动性：
- 标准差大：波动剧烈，比值不稳定
- 标准差小：波动平缓，比值相对稳定

#### 4. 分位数
当前值在历史数据中的排名百分比：
- 0-20%：历史低位
- 20-40%：偏低
- 40-60%：正常
- 60-80%：偏高
- 80-100%：历史高位

#### 5. 状态判断
- **偏高 ⬆️**：当前值 > 均值 + 1标准差（约前16%）
- **正常 ➡️**：在均值 ± 1标准差范围内（约68%）
- **偏低 ⬇️**：当前值 < 均值 - 1标准差（约后16%）

### 图表解读

#### 曲线颜色
- **金色**：金银比、金铂比（贵金属）
- **棕色**：油金比（能源）
- **蓝色**：纳指/标普比（科技）
- **红色**：道指/黄金比（股市vs避险）

#### 参考线
- **红色虚线**：历史平均值，长期中枢
- **橙色区域**：±1标准差，正常波动范围
- **浅红区域**：±2标准差，极端波动范围

#### 统计框
- 位置：左上角
- 颜色：
  - 红色边框：当前偏高
  - 绿色边框：当前偏低
  - 灰色边框：正常范围

## 实战案例

### 案例1：识别金银套利机会

**场景**：金银比处于历史高位

**分析步骤**：
1. 运行 `python gold_silver_ratio_chart.py`
2. 查看当前值和分位数
3. 如果分位数 > 80%，说明金银比偏高

**交易策略**：
- 做多白银，做空黄金（或买入白银，卖出黄金）
- 等待金银比回归均值
- 风险：比值可能继续上涨

**历史参考**：
- 2020年3月：金银比达到125（极端高位）
- 随后回落至70-80区间
- 套利空间约40%

### 案例2：判断市场风险偏好

**场景**：评估当前市场是风险偏好还是避险情绪

**分析步骤**：
1. 运行 `python analyze_all_ratios.py`
2. 重点关注：
   - 油金比
   - 道指/黄金比
3. 查看汇总报告

**判断标准**：
- **风险偏好高**：
  - 油金比偏高
  - 道指/黄金比偏高
  - 金银比正常或偏低
  
- **避险情绪浓**：
  - 油金比偏低
  - 道指/黄金比偏低
  - 金银比可能偏高

**当前市场（2026-02-09）**：
- 油金比：0.01（偏低，分位数1%）
- 道指/黄金比：9.97（偏低，分位数1%）
- **结论**：强烈的避险情绪

### 案例3：科技股vs大盘轮动

**场景**：判断科技股和价值股的相对强弱

**分析步骤**：
1. 运行 `python analyze_all_ratios.py`
2. 查看纳指/标普比
3. 观察趋势变化

**投资策略**：
- **纳指/标普比上升**：
  - 科技股强势
  - 增加科技股配置
  - 关注成长股
  
- **纳指/标普比下降**：
  - 大盘股强势
  - 增加价值股配置
  - 关注传统行业

**当前状态（2026-02-09）**：
- 纳指/标普比：3.62（正常，分位数74%）
- 略偏强势，科技股表现尚可

## 故障排除

### 问题1：MT5连接失败

**错误信息**：
```
❌ MT5初始化失败
```

**解决方案**：
1. 确认MT5终端已启动
2. 确认已登录账户
3. 检查MT5终端路径是否正确
4. 尝试重启MT5终端

### 问题2：品种不存在

**错误信息**：
```
❌ 品种不存在
```

**解决方案**：
1. 检查品种代码是否正确
2. 在MT5中搜索该品种
3. 确认账户有该品种的访问权限
4. 不同经纪商的品种代码可能不同：
   - XAUUSD vs GOLD
   - XAGUSD vs SILVER
   - US30 vs DJ30

### 问题3：数据不完整

**错误信息**：
```
✅ XAUUSD: 获取 100 条数据
```
（数据量明显偏少）

**解决方案**：
1. 减少时间范围（如从5年改为2年）
2. 检查MT5历史数据是否完整
3. 在MT5中手动下载历史数据：
   - 打开图表
   - 按Home键滚动到最早
   - 等待数据加载

### 问题4：图表不显示中文

**现象**：图表中的中文显示为方框

**解决方案**：
1. 确认系统已安装中文字体
2. 修改脚本中的字体设置：
```python
plt.rcParams['font.sans-serif'] = ['SimHei', 'Microsoft YaHei', 'Arial Unicode MS']
```

### 问题5：内存不足

**错误信息**：
```
MemoryError
```

**解决方案**：
1. 减少时间范围
2. 使用更大的时间周期（如H4改为D1）
3. 关闭其他程序释放内存
4. 分批分析，不要一次性分析所有比值

## 最佳实践

### 1. 定期分析
建议每周运行一次批量分析，跟踪比值变化趋势。

### 2. 结合多个比值
不要只看单一比值，综合多个比值判断市场状态。

### 3. 关注极端值
当比值达到±2标准差时，往往是重要的交易机会。

### 4. 保存历史数据
定期保存CSV文件，建立自己的历史数据库。

### 5. 回测验证
使用历史数据回测你的交易策略，验证有效性。

## 进阶技巧

### 1. 批处理自动化

创建批处理文件 `daily_analysis.bat`：
```batch
@echo off
cd /d %~dp0
python analyze_all_ratios.py
echo 分析完成，按任意键退出
pause
```

### 2. 定时任务

使用Windows任务计划程序，每天自动运行分析。

### 3. 数据对比

保存每周的汇总报告，对比变化：
```python
import pandas as pd

# 读取两周的数据
df1 = pd.read_csv('市场比值汇总_20260202.csv')
df2 = pd.read_csv('市场比值汇总_20260209.csv')

# 对比变化
print(df2['当前值'] - df1['当前值'])
```

### 4. 自定义警报

添加代码，当比值达到极端值时发送通知：
```python
if current > mean + 2 * std:
    print("⚠️ 警报：比值达到历史高位！")
    # 可以添加邮件或消息通知
```

## 相关资源

- [MetaTrader 5 Python文档](https://www.mql5.com/en/docs/python_metatrader5)
- [Pandas文档](https://pandas.pydata.org/docs/)
- [Matplotlib文档](https://matplotlib.org/stable/contents.html)

---

如有其他问题，欢迎提交Issue！
